// import { createAction, createReducer } from "@reduxjs/toolkit";

// const initialState = {
//     contacts: () => { return JSON.parse(window.localStorage.getItem('contacts')) ?? [] }
// }
// export const contactsReducer = createReducer(initialState, builder => {
//     builder.addCase('contacts/submit', (state, action) => {
//         return {
//             ...state,
            
//         }
//     });
// })